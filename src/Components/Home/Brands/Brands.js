import { Container, Grid } from "@mui/material";
import React from "react";

const Brands = () => {
  return (
    <Container maxWidth="xl">
      <Grid container spacing={{ xs: 1, md: 2 }}></Grid>
    </Container>
  );
};

export default Brands;
